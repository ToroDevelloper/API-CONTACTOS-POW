<!DOCTYPE html>
<html>
<head>
    <title>Estadísticas por Barrio</title>
    <meta charset="utf-8">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/estilos/estilos.css" rel="stylesheet">
    <style>
        .stat-card {
            background: white;
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 20px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }
        .barrio-name {
            font-weight: 600;
            font-size: 1.1rem;
            margin-bottom: 10px;
            color: #333;
        }
        .stat-info {
            display: flex;
            justify-content: space-between;
            margin-bottom: 8px;
            font-size: 0.9rem;
        }
        .progress {
            height: 30px;
            border-radius: 8px;
            background-color: #e9ecef;
        }
        .progress-bar {
            font-weight: 600;
            line-height: 30px;
            transition: width 1s ease-in-out;
        }
        .total-badge {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 15px 25px;
            border-radius: 10px;
            display: inline-block;
            margin-bottom: 30px;
            font-size: 1.2rem;
            font-weight: 600;
        }
    </style>
</head>
<body class="container mt-4">

<h1>📊 ESTADÍSTICAS POR BARRIO</h1>
<a href="/" class="btn btn-secondary mb-3">← Volver al Directorio</a>

<div class="total-badge">
    Total de Contactos: <%= totalContactos %>
</div>

<div class="row">
    <div class="col-md-10">
        <% barrios.forEach(function(b, index) {
            // Colores para las barras de progreso
            const colores = [
                'bg-primary', 'bg-success', 'bg-info',
                'bg-warning', 'bg-danger', 'bg-secondary',
                'bg-dark'
            ];
            const color = colores[index % colores.length];
        %>

        <div class="stat-card">
            <div class="barrio-name">
                <%= b.barrio || 'Sin barrio asignado' %>
            </div>

            <div class="stat-info">
                <span><strong><%= b.cantidad %></strong> contacto<%= b.cantidad !== 1 ? 's' : '' %></span>
                <span><strong><%= b.porcentaje %>%</strong></span>
            </div>

            <div class="progress">
                <div class="progress-bar <%= color %>"
                     role="progressbar"
                     style="width: <%= b.porcentaje %>%"
                     aria-valuenow="<%= b.porcentaje %>"
                     aria-valuemin="0"
                     aria-valuemax="100">
                    <%= b.porcentaje %>%
                </div>
            </div>
        </div>

        <% }); %>

        <% if (barrios.length === 0) { %>
            <div class="alert alert-info">
                No hay datos de barrios disponibles
            </div>
        <% } %>
    </div>
</div>

<script>
    // Animación de las barras de progreso al cargar
    window.addEventListener('load', function() {
        const progressBars = document.querySelectorAll('.progress-bar');
        progressBars.forEach(bar => {
            const width = bar.style.width;
            bar.style.width = '0%';
            setTimeout(() => {
                bar.style.width = width;
            }, 100);
        });
    });
</script>

</body>
</html>