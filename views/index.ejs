<!DOCTYPE html>
<html>
<head>
    <title>Contactos</title>
    <meta charset="utf-8">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/estilos/estilos.css" rel="stylesheet">
    <style>
        .header-actions {
            display: flex;
            gap: 10px;
            margin-bottom: 20px;
            flex-wrap: wrap;
        }
        .stats-badge {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border: none;
        }
        .stats-badge:hover {
            background: linear-gradient(135deg, #764ba2 0%, #667eea 100%);
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(102, 126, 234, 0.4);
        }
    </style>
</head>
<body class="container mt-4">

<h1>DIRECTORIO DE CONTACTOS</h1>

<div class="header-actions">
    <a href="/crearcontacto" class="btn btn-success">
        <i class="bi bi-plus-circle"></i> Nuevo Contacto
    </a>
    <a href="/estadisticas" class="btn btn-primary stats-badge">
        📊 Ver Estadísticas por Barrio
    </a>
</div>

<div class="table-responsive">
    <table class="table table-striped table-hover">
        <thead class="table-dark">
        <tr>
            <th>ID</th>
            <th>NOMBRE</th>
            <th>GÉNERO</th>
            <th>DIRECCIÓN</th>
            <th>TELÉFONO</th>
            <th>EMAIL</th>
        </tr>
        </thead>
        <tbody>
        <% if (contactos.length === 0) { %>
            <tr>
                <td colspan="6" class="text-center text-muted">
                    No hay contactos registrados. ¡Agrega el primero!
                </td>
            </tr>
        <% } else { %>
            <% contactos.forEach(function(c) { %>
                <tr>
                    <td><%= c.id_contacto %></td>
                    <td><%= c.primer_nombre %> <%= c.segundo_nombre || '' %> <%= c.primer_apellido %> <%= c.segundo_apellido || '' %></td>
                    <td><%= c.detalle_genero || '-' %></td>
                    <td><%= c.detalle_direccion || '-' %></td>
                    <td><%= c.telefono || '-' %></td>
                    <td><%= c.email || '-' %></td>
                </tr>
            <% }); %>
        <% } %>
        </tbody>
    </table>
</div>

<div class="mt-3 text-muted">
    <small>Total de contactos: <%= contactos.length %></small>
</div>

</body>
</html>